export enum EOderBy {
  ASC = "ASC",
  DESC = "DESC",
}

export enum ECardType {
  MONTH = "MONTH",
  DAY = "DAY",
}

export enum EVehicleType {
  MOTORBIKE = "MOTORBIKE",
  CAR = "CAR",
}

export enum ETypeHistory {
  IN = "IN",
  OUT = "OUT",
}
