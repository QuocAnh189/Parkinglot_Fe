# services:
#   app:
#     build:
#       context: .
#       args:
#         VITE_API_URL: "http://139.59.250.218:8002/api/v1"
#         # VITE_API_URL: "http://localhost:8080/api/v1"
#     ports:
#       - "8003:80"
#     environment:
#       - VITE_API_URL=http://139.59.250.218:8002/api/v1
#       # - VITE_API_URL=http://localhost:8080/api/v1
#     volumes:
#       - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
#       - .:/app
#     restart: unless-stopped

services:
  app:
    build:
      context: .
      args:
        VITE_API_URL: "https://goparking.duckdns.org/api/"
    ports:
      - "8003:80"
      - "443:443"
    environment:
      - VITE_API_URL=https://goparking.duckdns.org/api/
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - /etc/letsencrypt/live/goparking.duckdns.org/fullchain.pem:/etc/nginx/ssl/fullchain.pem:ro
      - /etc/letsencrypt/live/goparking.duckdns.org/privkey.pem:/etc/nginx/ssl/privkey.pem:ro
    restart: unless-stopped
